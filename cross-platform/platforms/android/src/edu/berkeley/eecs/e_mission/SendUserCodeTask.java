package edu.berkeley.eecs.e_mission;

/*
import java.io.IOException;
import java.net.URI;
import java.net.URISyntaxException;

import org.apache.http.HttpResponse;
import org.apache.http.client.methods.HttpGet;

import android.app.Service;
import android.net.http.AndroidHttpClient;
import android.os.AsyncTask;

public class SendUserCodeTask extends implements Runnable {

	@Override
	protected HttpResponse doInBackground(String... serverUri) {
        try {
        	HttpGet msg = new HttpGet(new URI(serverUri[0]));
        	System.out.println("Posting data to "+msg.getURI());
        	AndroidHttpClient connection = AndroidHttpClient.newInstance(R.class.toString());
        	HttpResponse response = connection.execute(msg);
        	System.out.println("Got response "+response+" with status "+response.getStatusLine());
        	connection.close();
        	return response;
        } catch (URISyntaxException e) {
        	// TODO Auto-generated catch block
        	e.printStackTrace();
        } catch (IOException e) {
        	// TODO Auto-generated catch block
        	e.printStackTrace();
        }
		return null;
	}
}
*/


